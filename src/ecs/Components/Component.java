package ecs.Components;

/**
 * Abstract base class for components in an Entity Component System architecture.
 * This class contains no members. It only exists to indicate that a derived class is a type of ECS component.
 * In an ECS system, a component should contain only data, and no behavior other than basic accessors for the data.
 */
public abstract class Component {
    /*
     * Intentionally empty class
     */
}
